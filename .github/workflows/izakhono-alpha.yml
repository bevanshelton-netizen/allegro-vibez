name: IZAKHONO Build to Alpha

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  alpha:
    # Owner-controlled, immutable IZAKHONO policy. Promotion remains health-gated.
    uses: bevanshelton-netizen/izakhono-builder/.github/workflows/izakhono-build-to-alpha.yml@9f97877aadaf93c795f4a66f8cc860dd2d721b39
    with:
      manifest_path: .izakhono.json
