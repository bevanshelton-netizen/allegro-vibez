import { useState } from 'react'
import { useAuth } from '../context/AuthContext'
export function ForgotPassword(){const{requestPasswordReset}=useAuth();const[email,setEmail]=useState('');const[msg,setMsg]=useState('');async function submit(e){e.preventDefault();const{error}=await requestPasswordReset(email);setMsg(error?error.message:'Password reset link sent.')}return <main className="auth-page"><section className="auth-card"><h1>Reset access</h1><form className="stack" onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="pill-button">Send reset link</button></form>{msg&&<p>{msg}</p>}</section></main>}
export function ResetPassword(){const{updatePassword}=useAuth();const[pw,setPw]=useState('');const[msg,setMsg]=useState('');async function submit(e){e.preventDefault();const{error}=await updatePassword(pw);setMsg(error?error.message:'Password updated. You can now log in.')}return <main className="auth-page"><section className="auth-card"><h1>Choose a new password</h1><form className="stack" onSubmit={submit}><label>New password<input type="password" minLength="10" value={pw} onChange={e=>setPw(e.target.value)} required/></label><button className="pill-button">Update password</button></form>{msg&&<p>{msg}</p>}</section></main>}
export function VerifyEmail(){return <main className="auth-page"><section className="auth-card"><p className="eyebrow">ONE MORE STEP</p><h1>Verify your email</h1><p>Open the verification message sent to your inbox, then return to ALLEGRO-VIBEZ.</p></section></main>}
export function AccessDenied(){return <main className="auth-page"><section className="auth-card"><h1>Access restricted</h1><p>Your account does not currently have permission to open this area.</p></section></main>}
