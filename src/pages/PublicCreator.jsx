import { useEffect,useState } from 'react'
import { useParams } from 'react-router-dom'
import { getArtistBySlug } from '../services/artistService'
export default function PublicCreator(){const{slug}=useParams();const[artist,setArtist]=useState(null);const[error,setError]=useState('');useEffect(()=>{getArtistBySlug(slug).then(setArtist).catch(e=>setError(e.message))},[slug]);if(error)return <main className="container page-pad"><div className="notice error">{error}</div></main>;if(!artist)return <main className="container page-pad"><div className="screen-state">Loading artist…</div></main>;return <main className="container page-pad"><section className="creator-hero"><div className="avatar mega">{artist.stage_name?.slice(0,1)}</div><div><p className="eyebrow">PUBLIC CREATOR PROFILE</p><h1>{artist.stage_name}</h1><p className="lede">{artist.biography||'Independent creator on ALLEGRO-VIBEZ.'}</p><div className="chips">{(artist.genres||[]).map(g=><span key={g}>{g}</span>)}</div><p>{artist.location}</p></div></section></main>}
