import { useState } from 'react'
import { Link } from 'react-router-dom'
import { useAuth } from '../context/AuthContext'
export default function Register(){const{signUp}=useAuth();const[form,setForm]=useState({fullName:'',displayName:'',email:'',password:'',role:'artist',accept:false});const[message,setMessage]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);const change=e=>setForm(v=>({...v,[e.target.name]:e.target.type==='checkbox'?e.target.checked:e.target.value}));async function submit(e){e.preventDefault();setError('');setMessage('');if(form.password.length<10)return setError('Use at least 10 characters for your password.');if(!form.accept)return setError('Please accept the Terms and Privacy Notice.');setBusy(true);const{error:err}=await signUp(form);setBusy(false);if(err)return setError(err.message);setMessage('Account created. Check your email to verify your account.')}return <main className="auth-page"><section className="auth-card wide"><p className="eyebrow">JOIN THE MOVEMENT</p><h1>Create your account</h1><form onSubmit={submit} className="form-grid"><label>Full name<input name="fullName" required value={form.fullName} onChange={change}/></label><label>Display name<input name="displayName" value={form.displayName} onChange={change}/></label><label>Email<input name="email" type="email" required value={form.email} onChange={change}/></label><label>Role<select name="role" value={form.role} onChange={change}><option value="artist">Artist</option><option value="fan">Fan</option><option value="manager">Manager</option><option value="label">Label</option></select></label><label className="full">Password<input name="password" type="password" required value={form.password} onChange={change}/></label><label className="check full"><input name="accept" type="checkbox" checked={form.accept} onChange={change}/> I accept the Terms and Privacy Notice.</label>{error&&<div className="notice error full">{error}</div>}{message&&<div className="notice success full">{message}</div>}<button className="pill-button full" disabled={busy}>{busy?'Creating…':'Create account'}</button></form><p><Link to="/login">Already registered?</Link></p></section></main>}
